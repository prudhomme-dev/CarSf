{% extends 'base.html.twig' %}

{% block title %}Editer énergie{% endblock %}

{% block body %}
    <div class="row pt-3">
        <h1>Edition Type Energie</h1>

        {{ include('energy/_form.html.twig', {'button_label': 'Update'}) }}

        <div class="row mt-3">
            {{ include('energy/_delete_form.html.twig') }}
        </div>
        <a href="{{ path('app_energy_index') }}" class="btn btn-outline-danger mt-2">Retour à la liste</a>

    </div>
{% endblock %}
