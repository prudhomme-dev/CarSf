{% extends 'base.html.twig' %}

{% block title %}Editer une marque{% endblock %}

{% block body %}
    <div class="row pt-3">
        <h1>Editer une marque</h1>

        {{ include('brand/_form.html.twig', {'button_label': 'Update'}) }}
        <div class="row mt-3">
            {{ include('brand/_delete_form.html.twig') }}
        </div>
        <a href="{{ path('app_brand_index') }}" class="btn btn-outline-danger mt-2">Retour à la liste</a>


    </div>
{% endblock %}
